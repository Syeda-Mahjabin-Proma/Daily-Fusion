<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamePlay"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity android:name=".View.chooseWorkView" />
        <activity android:name=".View.calcBmiView" />
        <activity android:name=".View.healthCheckView" />
        <activity android:name=".View.calcBmrView" />
        <activity android:name=".View.calcWaterView" />
        <activity android:name=".View.calcBsaView" />
        <activity android:name=".View.calcCalBurntView" />
        <activity android:name=".View.gamePlayView" />
        <activity android:name=".View.gameRightWrongView" />
        <activity android:name=".View.gameTicTacToeView" />
        <activity android:name=".View.gameMemoryView" />
        <activity android:name=".View.gameWordScrambleView" />
        <activity android:name=".View.gameRockPaperScissorView" />
        <activity android:name=".View.moneyChangeView" />
        <activity android:name=".View.motivationView" />
        <activity android:name="toDoView" />
    </application>

</manifest>